# src

These are the files that you need to modify. `commands.ts` contains all of the commands available. `app.ts` contains the actual parsing code. This project uses TypeScript, so new files should use `.ts` extension and TypeScript syntax.
